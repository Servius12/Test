# Полная Инструкция по Настройке Telegram Бота

## Шаг 1: Создание Telegram Бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите название бота (например: "Фитнес Тренер")
4. Введите username бота (должен заканчиваться на `bot`, например: `fitness_trainer_bot`)
5. **Сохраните токен бота** - он понадобится позже

## Шаг 2: Настройка Web App

1. Отправьте команду `/setmenubutton` в BotFather
2. Выберите вашего бота
3. Нажмите "Edit Menu Button"
4. Введите текст кнопки: `Открыть приложение`
5. Введите URL приложения: `https://ваш-домен.com/register.html`

## Шаг 3: Настройка Команд Бота

1. Отправьте команду `/setcommands` в BotFather
2. Выберите вашего бота
3. Вставьте следующие команды:

```
start - Начать работу с ботом
help - Помощь и инструкции
profile - Открыть мой профиль
workouts - Мои тренировки
admin - Панель администратора
```

## Шаг 4: Настройка Описания

1. Отправьте `/setdescription` в BotFather
2. Введите описание:

```
Персональный фитнес-тренер в вашем кармане!

🏋️ Программы тренировок
📊 Отслеживание прогресса
📈 Статистика и графики
👨‍⚕️ Связь с тренером
```

## Шаг 5: Установка Изображения Бота

1. Отправьте `/setuserpic` в BotFather
2. Загрузите изображение для аватара бота (квадратное, минимум 150x150px)

## Шаг 6: Развертывание Приложения

### Вариант A: Использование Trickle Hosting

1. В панели Trickle нажмите "Deploy"
2. Скопируйте предоставленный URL
3. Вернитесь к BotFather и обновите URL в `/setmenubutton`

### Вариант B: Собственный Хостинг

1. Разверните все файлы на вашем сервере
2. Убедитесь, что HTTPS настроен (обязательно для Telegram Web Apps)
3. Обновите URL в настройках бота

## Шаг 7: Настройка Логики Бота (Python)

Создайте файл `bot.py` со следующим кодом:

```python
from telegram import Update, WebAppInfo
from telegram.ext import Application, CommandHandler, ContextTypes

BOT_TOKEN = "ВАШ_ТОКЕН_БОТА"
WEB_APP_URL = "https://ваш-домен.com"

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text(
        "👋 Добро пожаловать в Фитнес Тренер!\n\n"
        "Нажмите кнопку ниже, чтобы открыть приложение:",
        reply_markup={
            "inline_keyboard": [[
                {"text": "🏋️ Открыть приложение", 
                 "web_app": {"url": f"{WEB_APP_URL}/register.html"}}
            ]]
        }
    )

async def profile(update: Update, context: ContextTypes.DEFAULT_TYPE):
    user_id = update.effective_user.id
    await update.message.reply_text(
        "Открываю ваш профиль...",
        reply_markup={
            "inline_keyboard": [[
                {"text": "👤 Мой профиль", 
                 "web_app": {"url": f"{WEB_APP_URL}/client.html"}}
            ]]
        }
    )

async def workouts(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text(
        "Открываю тренировки...",
        reply_markup={
            "inline_keyboard": [[
                {"text": "💪 Мои тренировки", 
                 "web_app": {"url": f"{WEB_APP_URL}/client.html"}}
            ]]
        }
    )

async def admin(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text(
        "Открываю панель администратора...",
        reply_markup={
            "inline_keyboard": [[
                {"text": "⚙️ Админ-панель", 
                 "web_app": {"url": f"{WEB_APP_URL}/admin.html"}}
            ]]
        }
    )

async def help_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text(
        "📱 Как использовать бота:\n\n"
        "1. Нажмите кнопку внизу 'Открыть приложение'\n"
        "2. Зарегистрируйтесь или войдите\n"
        "3. Заполните профиль\n"
        "4. Получите программу от тренера\n\n"
        "Команды:\n"
        "/profile - Открыть профиль\n"
        "/workouts - Мои тренировки\n"
        "/admin - Админ-панель\n"
        "/help - Эта справка"
    )

def main():
    app = Application.builder().token(BOT_TOKEN).build()
    
    app.add_handler(CommandHandler("start", start))
    app.add_handler(CommandHandler("profile", profile))
    app.add_handler(CommandHandler("workouts", workouts))
    app.add_handler(CommandHandler("admin", admin))
    app.add_handler(CommandHandler("help", help_command))
    
    print("🤖 Бот запущен!")
    app.run_polling()

if __name__ == '__main__':
    main()
```

## Шаг 8: Установка Зависимостей

Создайте `requirements.txt`:

```
python-telegram-bot==20.7
```

Установите:
```bash
pip install -r requirements.txt
```

## Шаг 9: Запуск Бота

```bash
python bot.py
```

## Шаг 10: Тестирование

1. Найдите вашего бота в Telegram
2. Нажмите `/start`
3. Нажмите кнопку "Открыть приложение"
4. Зарегистрируйтесь как клиент или тренер
5. Проверьте все функции

## Дополнительные Настройки

### Уведомления Администратору

Добавьте в `bot.py`:

```python
ADMIN_CHAT_ID = "ВАШ_TELEGRAM_ID"

async def notify_admin_new_registration(user_data):
    await application.bot.send_message(
        chat_id=ADMIN_CHAT_ID,
        text=f"🆕 Новая заявка на регистрацию!\n\n"
             f"Имя: {user_data['firstName']} {user_data['lastName']}\n"
             f"Username: @{user_data['username']}\n"
             f"Роль: {user_data['role']}\n\n"
             f"Одобрите в админ-панели: {WEB_APP_URL}/admin.html"
    )
```

### Постоянное Меню (Persistent Menu)

В BotFather используйте команду `/setmenubutton` и настройте кнопку меню, которая всегда будет внизу чата.

## Проблемы и Решения

### Проблема: "Web App не открывается"
**Решение:** Убедитесь, что у вас настроен HTTPS. Telegram требует защищенное соединение.

### Проблема: "Данные пользователя не передаются"
**Решение:** Проверьте, что вы используете `window.Telegram.WebApp.initData` в JavaScript.

### Проблема: "Бот не отвечает"
**Решение:** 
1. Проверьте, что бот запущен (`python bot.py`)
2. Проверьте токен бота
3. Проверьте интернет-соединение сервера

## Полезные Ссылки

- [Документация Telegram Bot API](https://core.telegram.org/bots/api)
- [Документация Web Apps](https://core.telegram.org/bots/webapps)
- [python-telegram-bot](https://docs.python-telegram-bot.org/)

## Структура URL

- Регистрация: `https://ваш-домен.com/register.html`
- Вход: `https://ваш-домен.com/login.html`
- Главная: `https://ваш-домен.com/index.html`
- Клиент: `https://ваш-домен.com/client.html`
- Тренер: `https://ваш-домен.com/trainer.html`
- Админ: `https://ваш-домен.com/admin.html`

## Доступы

### Администратор
- Логин: `admin123`
- Пароль: `admin123`
- URL: `https://ваш-домен.com/login.html`

### Обычные пользователи
- Регистрация через бота → Одобрение админом → Вход

---

**Дата создания:** Ноябрь 2025
**Версия:** 1.0